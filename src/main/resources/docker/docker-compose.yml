services:
  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: temppassword
      MYSQL_DATABASE: start_db
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "3307:3306"
